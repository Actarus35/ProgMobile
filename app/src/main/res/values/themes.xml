<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Thème par défaut de l'application qui prend en charge DayNight (mode clair/sombre) -->
    <style name="Theme.MyGamesList" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Personnalisation des couleurs et autres éléments de style -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorOnBackground">@color/white</item>
        <item name="colorSurface">@color/white</item>
        <item name="colorOnSurface">@color/black</item>
    </style>

</resources>